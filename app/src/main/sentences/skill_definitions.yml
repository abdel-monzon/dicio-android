skills:
  - id: current_time
    specificity: high
    sentences:
      - id: query

  - id: calculator
    specificity: medium
    sentences:
      - id: calculate
        captures:
          - id: calculation
            type: string
  - id: calculator_operators
    specificity: low
    sentences:
      - id: addition
      - id: subtraction
      - id: multiplication
      - id: division
      - id: power
      - id: square_root

  - id: lyrics
    specificity: high
    sentences:
      - id: query
        captures:
          - id: song
            type: string

  - id: navigation
    specificity: high
    sentences:
      - id: query
        captures:
          - id: where
            type: string

  - id: media
    specificity: high
    sentences:
      - id: play
      - id: pause
      - id: previous
      - id: next

  - id: joke
    specificity: high
    sentences:
      - id: command

  - id: open
    specificity: medium
    sentences:
      - id: query
        captures:
          - id: what
            type: string

  - id: listening
    specificity: high
    sentences:
      - id: stop
      - id: start

  - id: search
    specificity: low
    sentences:
      - id: query
        captures:
          - id: what
            type: string

  - id: telephone
    specificity: low
    sentences:
      - id: dial
        captures:
          - id: who
            type: string
            
  - id: timer
    specificity: high
    sentences:
      - id: set
        input: [establece, un, temporizador, de, .duration.]
        captures:
          - id: duration
            type: duration
          - id: name
            type: string
      - id: set_named
        input: [establece, un, temporizador, de, .duration., llamado, .name.]
        captures:
          - id: duration
            type: duration
          - id: name
            type: string
      - id: cancel
        input: [cancela, el, temporizador]
        captures:
          - id: name
            type: string
      - id: cancel_named
        input: [cancela, el, temporizador, .name.]
        captures:
          - id: name
            type: string
      - id: query
        input: [cuánto, tiempo, queda, en, el, temporizador]
        captures:
          - id: name
            type: string
      - id: query_named
        input: [cuánto, tiempo, queda, en, el, temporizador, .name.]
        captures:
          - id: name
            type: string

  - id: util_yes_no
    specificity: high
    sentences:
      - id: yes
      - id: no

  - id: weather
    specificity: high
    sentences:
      - id: current
        captures:
          - id: where
            type: string

  - id: age
    specificity: medium
    sentences:
      - id: ask
      
  - id: translation
    specificity: high
    sentences:
      - id: translate
        captures:
          - id: query
            type: string
          - id: source
            type: string
          - id: target
            type: string
